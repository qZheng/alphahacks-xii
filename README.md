Run the flask.
```
flask db init
flask db migrate
flask db upgrade
flask run --host 127.0.0.1. --port 5000
```
The ip is hard coded. If you want it to run, switch it with whatever ipv4 address you got. 
Download the app to ios.
Run the app on your device.
